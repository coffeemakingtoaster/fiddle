sets:
  - name: test1
    code: print("hello")
  - name: test2
    code: |
      print("multiline hello")
      print("multiline hello2")
  - name: test3
    code: |
      # This function is only defined in the wrapper and imported via ctypes. Will this work?
      print(get_context())
